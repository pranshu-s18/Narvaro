<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/login">

    <fragment
        android:id="@+id/login"
        android:name="in.co.prototek.narvaro.Login"
        android:label="Login"
        tools:layout="@layout/fragment_login" />

    <fragment
        android:id="@+id/fingerprint"
        android:name="in.co.prototek.narvaro.Fingerprint"
        android:label="Fingerprint"
        tools:layout="@layout/fragment_fingerprint" />

    <fragment
        android:id="@+id/attendance"
        android:name="in.co.prototek.narvaro.Attendance"
        android:label="Attendance"
        tools:layout="@layout/fragment_attendance" />

    <fragment
        android:id="@+id/settings"
        android:name="in.co.prototek.narvaro.Settings"
        android:label="Settings" />

</navigation>